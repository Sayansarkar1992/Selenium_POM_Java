package BaseTests;

public class Constants {
    public static String CURRENT_DIR = System.getProperty("user.dir");
    public static String CONFIGURATION_YAML_PATH = CURRENT_DIR + "/src/test/resources/config.yml";
}
